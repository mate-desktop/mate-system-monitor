faces = [
  ['download.svg'],
  ['side.png'],
  ['upload.svg'],
]
foreach face: faces
   install_data(face,
     install_dir : join_paths(get_option('datadir'), 'pixmaps', 'mate-system-monitor')
   )
endforeach
